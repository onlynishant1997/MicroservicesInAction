spring:
  application:
     name: userservice
  profiles:
    active: default
  cloud:
     config:
      uri: http://localhost:8081
      
eureka:
   instance:
      preferIpAddress: true
   client:
      registerWithEureka: true
      fetchRegistry: true